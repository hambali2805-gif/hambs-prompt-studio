export function buildSeedancePrompt(ctx,scene,index){
 return `Seedance 2.0 motion prompt. Scene ${index+1}/${ctx.totalScenes}: ${scene.title}. Core visual: ${scene.description}. Motion mechanics must be explicit: ${(ctx.rules.motions||[]).slice(index,index+3).concat(scene.mustInclude||[]).filter(Boolean).join(', ')}. Maintain temporal consistency: same subject, same product scale, believable hand/foot/body movement, no teleporting product. Mode separation: ${ctx.mode==='ugc'?'phone-camera UGC realism, imperfect timing, natural micro-pauses':'controlled ad motion, clean transitions, premium product choreography'}. Presentation: ${ctx.presentation.label}, motion rules: ${ctx.presentation.motionRules.join(', ')}. Product reference focus: ${(ctx.rules.referenceFocus||[]).join(', ')}. Negative: ${ctx.negativePrompt}.`;
}
