export function buildContinuityRules(mode) {
    return mode === 'ugc'
        ? 'Maintain consistent character and product across frames, but keep natural handheld imperfection and real creator timing.'
        : 'Maintain strict character, wardrobe, product, lighting, and environment continuity like a polished campaign.';
}
